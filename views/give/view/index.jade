extends ../../../layouts/default  

block head
	title #{result.compartio.title} disponible en #{result.city.name} - compart.io
block neck
	link(rel='stylesheet', href='/views/give/view/index.min.css?#{cacheBreaker}')

block body
	div.page
		div.container
			h1 #{result.compartio.title} 
			p 
				| disponible en 
				a(href='/#{result.city.slug}') #{result.city.name}
			p
				img.compartio-image(src="#{result.compartio.url_image}", alt="#{result.compartio.title}")
			p #{result.user.username}
			p --- Compartio
			p Descripción 
			p #{result.compartio.description}
			p Estado
			p #{result.compartio.status}
			p Ciudad: #{result.city.name}
			p --- Usuarios interesados
			if result.interested_users != null
				p Usuarios Interesados
				for obj in result.interested_users
					a(href='/user/#{obj._id}', title='Ir al perfil de #{obj.username}')
						img.interested_users(src='#{obj.url_image}', alt='#{obj.username}')  
			p --- Mensajes
			if result.message != null
					for obj in result.message
						p Mensaje: #{obj.content}
						for userobj in result.user
							if userobj._id.toString() == obj.user_id.toString()
								p User Name: #{userobj.username}
			p --- Categoría
			p Categoría: #{result.category.title}
			// p Categoría: #{result.compartio.category_id.title}
			p #{result.category.description}
